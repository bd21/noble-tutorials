syntax = "proto3";

package circle.cctp.v1;

option go_package = "github.com/circlefin/noble-cctp-private-builds/x/cctp/types";

/**
 * Message format for BurningAndMintingPaused
 * @param paused true if paused, false if not paused
 */
message BurningAndMintingPaused { bool paused = 1; }
